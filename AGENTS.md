# KianPrint2 — Codex作業指示

このフォルダが独立した製品リポジトリ。親の指示の自動読み込みには依存しない。
作業開始時に `/Users/hiroki/Development/AGENTS.md` を読む。
動作確認・インストール・コミット・push・リリースの依頼では、実行前に
`/Users/hiroki/Development/RELEASING.md` も必ず読む。パスの再指定をユーザーに求めない。

「動作確認できる状態にして」は、この製品の検証・ビルド・ローカルインストールまで。
「この変更をリリースして」は、この製品の正式リリース工程全体を指す。
詳しい工程・完了条件は上記共通ファイルと下記個別手順に従う。
他製品への操作や `--all` は、ユーザーが全体操作を明示した場合だけ行う。
共通ファイルが読めない環境では、個別手順とこの対象限定ルールを使って進め、
不足する公開先などの必須情報だけを確認する。

必ず `Docs/Releasing.md` と `README.md` を読む。β版・Universal Binary・GitHub Releaseの個別手順を使う。共通のDrive用publisherだけで正式公開完了としない。既にPublicなら初回Public化の工程は不要。リポジトリの可視性変更は通常のリリース指示に含めない。
